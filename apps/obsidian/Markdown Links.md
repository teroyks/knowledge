A custom plugin for editing a Markdown directory or an Eleventy site in Obsidian.

- Use the main directory as a vault for normal Markdown.
- Point the vault to an `src` folder with Eleventy content.
    - Set the content root setting to `src` to get the URLs right.
